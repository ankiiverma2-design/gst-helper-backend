# URL of the deployed backend API. For local dev the default is http://localhost:3000.
# In production set this to your deployed backend, e.g. https://gst-helper-backend.onrender.com
VITE_API_BASE=http://localhost:3000
